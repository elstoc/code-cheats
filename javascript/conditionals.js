/**********************************/
/* Conditional execution using && */
/**********************************/

(x === 1) && execute_function();

/********************/
/* if...then...else */
/********************/

if(x === 1) statement;

if(x === 1)
    //Execute some code
else if(x === 2)
    //Execute some code
else
    //Execute some code

if(x === 1) {
    //Execute some code
} else if(x === 2) {
    //Execute some code
} else {
    //Execute some code
}

/**********/
/* Switch */
/**********/

switch(x) {
    case 1:
        // Execute some code
        break;
    case 2:
        // Execute some code
        break;
    default:
        // Execute some code
        break;
}

/********************/
/* Ternary Operator */
/********************/

x === 1 ? do_something : do_something_else;
